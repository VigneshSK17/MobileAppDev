import 'package:flutter/material.dart';

class Constants {
      static const primarySwatch = Colors.red;

      // Light Theme (l for light, c for color
      static const Color lCBackground = Color(0xffBCD1);

      static const Color bCForeground = Color(0xFFEFEFEF);
}